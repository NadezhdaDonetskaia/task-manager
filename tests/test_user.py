import pytest
from task_manager.logger_config import logger


# @pytest.mark.django_db
# def test_content_without_aut(client):
#     request = client.get('/')
#     logger.error(request.text_content)
#     assert 'Вход' in request.text_content
